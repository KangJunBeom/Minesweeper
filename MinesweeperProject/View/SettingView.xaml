<UserControl x:Class="MinesweeperProject.View.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White">
    <UserControl.Resources>
        <Style x:Key="LIGBackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="-3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LIGComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="MainGrid" SnapsToDevicePixels="true">
                            <ToggleButton x:Name="ToggleButton" 
                                  BorderBrush="{TemplateBinding BorderBrush}" 
                                  BorderThickness="{TemplateBinding BorderThickness}" 
                                  Background="{TemplateBinding Background}" 
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                  Grid.ColumnSpan="2"
                                  Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" CornerRadius="18" 
                                        Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="40"/>
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow" Grid.Column="1" 
                                              Fill="#002C77" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                              Data="M 0 0 L 5 5 L 10 0 Z" Margin="0,2,0,0"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#002C77"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" 
                                      Content="{TemplateBinding SelectionBoxItem}" 
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" 
                                      Margin="{TemplateBinding Padding}" 
                                      VerticalAlignment="Center" HorizontalAlignment="Left"/>

                            <Popup x:Name="Popup" Placement="Bottom" 
                           IsOpen="{TemplateBinding IsDropDownOpen}" 
                           AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border x:Name="DropDownBorder" Background="White" 
                                BorderBrush="#002C77" BorderThickness="1.5" CornerRadius="12" 
                                MinWidth="{Binding ActualWidth, ElementName=MainGrid}" MaxHeight="200" Margin="0,5,0,0">
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="ToggleButton" Property="BorderBrush" Value="#002C77"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="itemBorder" Background="Transparent" CornerRadius="8" Margin="2">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="#EBF2FF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="itemBorder" Property="Background" Value="#002C77"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LIGSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="BackgroundBorder" Background="#E0E0E0" CornerRadius="13" SnapsToDevicePixels="True">
                            <Ellipse x:Name="Thumb" Fill="White" Width="20" Height="20" HorizontalAlignment="Left" Margin="3">
                                <Ellipse.RenderTransform>
                                    <TranslateTransform X="0"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#002C77"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" 
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                                                             To="24" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" 
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                                                             To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LIGSliderStyle" TargetType="Slider">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid x:Name="Root">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="6" Background="#002C77" CornerRadius="3"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="18" Height="18">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="White" Stroke="#002C77" StrokeThickness="3"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="6" Background="#E0E0E0" CornerRadius="3"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top" Margin="30,25,30,10">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="LIGNex1" Foreground="#002C77" FontWeight="Bold" FontSize="13" Margin="0,0,0,2"/>
                <TextBlock Text="환경 설정" Foreground="#1A1A1A" FontWeight="ExtraBold" FontSize="34"/>
            </StackPanel>

            <Button Command="{Binding ReturnToMenuCommand}" 
                    Style="{StaticResource LIGBackButtonStyle}"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Width="40" Height="40">
                <TextBlock Text="←" FontSize="26" FontWeight="Bold" Foreground="#333" Margin="0,-4,0,0"/>
            </Button>
        </Grid>

        <StackPanel Margin="30,10,30,60">
            <Border Background="#F8F9FA" CornerRadius="12" Padding="14" Margin="0,0,0,15" BorderBrush="#EEEEEE" BorderThickness="1">
                <DockPanel>
                    <StackPanel>
                        <TextBlock Text="마스터 사운드" FontSize="15" FontWeight="Bold" Foreground="#333"/>
                        <TextBlock Text="전체 음향 시스템 제어" FontSize="11" Foreground="#999" Margin="0,1,0,0"/>
                    </StackPanel>
                    <ToggleButton IsChecked="{Binding IsMasterSoundOn}" 
                                  Style="{StaticResource LIGSwitchStyle}" 
                                  HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>

            <TextBlock Text="배경음악 선택" FontSize="13" FontWeight="Bold" Foreground="#002C77" Margin="5,0,0,6"/>
            <ComboBox Style="{StaticResource LIGComboBoxStyle}" 
                      ItemsSource="{Binding BgmList}" 
                      SelectedItem="{Binding SelectedBgm}"
                      DisplayMemberPath="Title"
                      Height="42" Margin="0,0,0,15"/>

            <CheckBox Content="효과음(SFX) 출력 활성화" 
                      IsChecked="{Binding IsSfxEnabled}" 
                      FontSize="13" FontWeight="SemiBold"
                      Margin="5,0,0,15"/>

            <Separator Background="#F0F0F0" Margin="0,0,0,15"/>

            <StackPanel Margin="0,0,0,15">
                <DockPanel Margin="5,0,5,6">
                    <TextBlock Text="BGM 볼륨" FontSize="13" FontWeight="Bold" Foreground="#555"/>
                    <TextBlock Text="{Binding BgmVolume, StringFormat={}{0:0}}" 
                               HorizontalAlignment="Right" 
                               Foreground="#002C77" 
                               FontWeight="Bold"/>
                </DockPanel>
                <Slider Style="{StaticResource LIGSliderStyle}" 
                        Minimum="0" Maximum="100" 
                        Value="{Binding BgmVolume}"
                        IsSnapToTickEnabled="True"
                        TickFrequency="1"/>
            </StackPanel>

            <StackPanel Margin="0,5,0,10">
                <DockPanel Margin="5,0,5,10">
                    <TextBlock Text="SFX 볼륨" FontSize="13" FontWeight="Bold" Foreground="#555"/>
                    <TextBlock Text="{Binding SfxVolume, StringFormat={}{0:0}}" 
                               HorizontalAlignment="Right" 
                               Foreground="#002C77" 
                               FontWeight="Bold"/>
                </DockPanel>
                <Slider Style="{StaticResource LIGSliderStyle}" 
                        Minimum="0" Maximum="100" 
                        Value="{Binding SfxVolume}"
                        IsSnapToTickEnabled="True"
                        TickFrequency="1"/>
            </StackPanel>
        </StackPanel>
    </DockPanel>
</UserControl>